name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      # The focused step below keeps native case-policy evidence visible.
      - run: cargo test --workspace -- --skip mv_batch_equivalent_names
      - name: Verify native move destination case policy
        run: cargo test -p hyalo-cli --test e2e mv_batch_equivalent_names -- --nocapture
      - name: Verify CLI runs
        run: cargo run -p hyalo-cli -- --help

  # Dogfood: lint the project's own knowledgebase and surface any schema/body
  # violations as inline PR annotations. `.hyalo.toml` at the repo root sets
  # `dir = "hyalo-knowledgebase"`, so `hyalo lint` targets the vault, and
  # `--format github` emits `::error`/`::warning` workflow commands. Paths are
  # repo-root-relative because the job runs from the checkout root. The lint
  # exits non-zero on errors, failing the check. Annotations are
  # platform-independent, so this runs on ubuntu only.
  #
  # The binary comes from the published setup-hyalo action (latest release)
  # rather than a source build — this doubles as an end-to-end dogfood of the
  # release-artifact install path (iter-171). Unlike the other actions here,
  # setup-hyalo is deliberately NOT pinned to a SHA: floating on the
  # first-party mutable `@v1` tag is the recorded design (DEC-051) and exactly
  # what consumers run — which is the point of the dogfood.
  #
  # DIFF-AWARE (iter-186): on a PR this lints only the markdown files the PR
  # actually touches (`git diff | hyalo lint --files-from -`). GitHub registers
  # at most 10 warning + 10 error annotations per step, so scoping to the PR's
  # own files spends that budget on findings the author can act on — instead of
  # letting a vault-wide backlog exhaust the cap first (the iter-171 failure).
  # DELIBERATE GAP: a PR that breaks *other* files (e.g. deletes a note others
  # link to, or a schema change) passes this diff-aware check because those
  # files aren't in the diff; the `lint-kb-full` job below catches such
  # cross-file regressions on every push to main, where annotation caps don't
  # matter. Annotations belong to the PR's own files by design.
  lint-kb:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      # fetch-depth: 0 gives full history so the merge-base with the PR base is
      # available for a three-dot diff (see below).
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      - uses: ractive/setup-hyalo@v1
      - name: Lint changed knowledgebase files
        # Three-dot `A...HEAD` diffs against the merge-base of the PR base and
        # HEAD, so a stale branch is annotated only for files IT changed — not
        # for base-tip drift it never touched (two-dot would do the latter).
        # `--diff-filter=d` drops deleted paths (nothing to lint); a docs-
        # untouched PR yields an empty list and `--files-from -` exits 0
        # ("0 files checked"), keeping the job green. `${{ github.base_ref }}`
        # (not a hard-coded `main`) keeps this correct for any base branch.
        run: |
          git fetch --quiet origin "${{ github.base_ref }}"
          git diff --name-only --diff-filter=d "origin/${{ github.base_ref }}...HEAD" \
            | hyalo lint --strict --files-from - --format github

  # FULL-VAULT (iter-186): the diff-aware PR job above never sees cross-file
  # regressions, so full-vault `--strict` enforcement runs here on every push to
  # main — annotation caps are irrelevant on a post-merge push (no PR diff to
  # annotate), and this is the safety net for the deliberate gap documented on
  # `lint-kb`. This is the vault-health gate that used to live on the PR job.
  lint-kb-full:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: ractive/setup-hyalo@v1
      - name: Lint full knowledgebase
        run: hyalo lint --strict --format github

  quality-gates:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
      - name: Check feature fanout
        run: cargo run -p xtask -- check-feature-fanout
      - name: Check help drift
        run: cargo run -p xtask -- check-help-drift
      - name: Check every subcommand is in COMMAND REFERENCE
        run: cargo run -p xtask -- check-command-reference
      - name: Check bundled skills pass the skills profile
        run: cargo run -p xtask -- check-bundled-skills
      - name: Check pi-package vendored copies are in sync
        run: cargo run -p xtask -- check-pi-package-sync
      - name: Check Rust-derived TypeScript declarations
        run: cargo run -p xtask -- check-ts-types
      - name: Check generated Pi API runtime
        run: |
          npm --prefix npm/hyalo ci --ignore-scripts
          cargo run -p xtask -- check-pi-runtime
      - name: Check Codex plugin assets and metadata
        run: cargo run -p xtask -- check-codex-package
      - name: Check every shipped --jq recipe executes
        run: cargo run -p xtask -- check-jq-recipes
      - name: Check mutating commands go through MutationJournal
        run: cargo run -p xtask -- check-mutation-journal
      - name: Check typed command output contracts
        run: cargo run -p xtask -- check-typed-output
